# Semantic retrieval

lexical検索は常に利用できます。semantic検索は任意のDSH runtime capabilityです。

DSH pluginの公開package boundaryは`./dsh` entrypointに限定されています。
optional embeddingの詳細はDSH runtimeとverification reportの契約です。
