# Semantic retrieval

Lexical retrieval always remains available. Semantic retrieval is an optional
DSH runtime capability.

The DSH plugin keeps its public package boundary limited to the `./dsh`
entrypoint. Optional embedding behavior belongs to the DSH runtime and its
verification reports.
